 {
   "name": "5-min-project",
   "version": "1.0.0",
   "private": true,
   "description": "Minimal T3 Chat-like clone ready for Netlify (static frontend + serverless function).",
   "scripts": {
     "build": "echo 'No build step'",
     "serve": "npx netlify-cli@latest dev",
     "start": "npx netlify-cli@latest dev"
   },
   "dependencies": {},
   "engines": {
     "node": ">=18"
   }
 }


